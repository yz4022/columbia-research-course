title: Columbia Research
description: Columbia Research Project
template:
  repo: columbia-research-template
  name: columbia-research
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
